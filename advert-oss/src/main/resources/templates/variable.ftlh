<#-- 版本号 更新资源 防止缓存-->
<#assign V = "1.2">
<#-- 页面Head标题-->
<#assign Title = "APP Store 平台">
<#-- 项目根目录-->
<#assign RootPath=rc.contextPath>
<#-- 页面根目录-->
<#assign Base=(RootPath == "") ? string("/" ,RootPath)>